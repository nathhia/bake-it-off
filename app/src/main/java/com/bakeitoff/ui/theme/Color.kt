package com.bakeitoff.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Paleta usada nos componentes de receita (cards, chips de filtro)
val LightPink = Color(0xFFF5CFE4)
val LightPurple = Color(0xFFD6DCF2)
val CardBackground = Color(0xFFF0F2F7)

// Roxo/azulado principal da marca — capturado do dynamic color no celular da
// usuária (papel de parede dela) e fixado aqui pra não variar por aparelho.
val BrandPrimary = Color(0xFF475D92)
// Fundo do círculo do cupcake na tela inicial (era MaterialTheme.colorScheme.primaryContainer).
val BrandPrimaryContainer = Color(0xFFD9E2FF)
// Fundo do chip de filtro/tag quando selecionado (era colorScheme.secondaryContainer).
val BrandSecondaryContainer = Color(0xFFDCE2F9)
// Fundo dos cards padrão (ex: "Dica do vídeo", ingredientes na edição) — era colorScheme.surfaceVariant.
val BrandSurfaceVariant = Color(0xFFE1E2EC)
// Fundo das barras de topo (TopAppBar) — era colorScheme.surface.
val BrandSurface = Color(0xFFEBEBF6)
// Fundo geral das telas — era colorScheme.background.
val BrandBackground = Color(0xFFFAF8FF)