package com.bakeitoff.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Palette used in the recipe components (cards, filter chips)
val LightPink = Color(0xFFF5CFE4)
val LightPurple = Color(0xFFD6DCF2)
val CardBackground = Color(0xFFF0F2F7)

// Main brand purple/blue — captured from dynamic color on the user's phone
// (her wallpaper) and fixed here so it doesn't vary by device.
val BrandPrimary = Color(0xFF475D92)
// Background of the cupcake circle on the home screen (was MaterialTheme.colorScheme.primaryContainer).
val BrandPrimaryContainer = Color(0xFFD9E2FF)
// Background of the filter/tag chip when selected (was colorScheme.secondaryContainer).
val BrandSecondaryContainer = Color(0xFFDCE2F9)
// Background of the standard cards (e.g. "Dica do vídeo", ingredients in edit mode) — was colorScheme.surfaceVariant.
val BrandSurfaceVariant = Color(0xFFE1E2EC)
// Background of the top bars (TopAppBar) — was colorScheme.surface.
val BrandSurface = Color(0xFFEBEBF6)
// Overall screen background — was colorScheme.background.
val BrandBackground = Color(0xFFFAF8FF)